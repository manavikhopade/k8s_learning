apiVersion: v1
kind: PersistentVolume
metadata:
  name: fitcheck-pv
spec:
  capacity:
    storage: 1Gi
  accessModes:
    - ReadWriteOnce
  persistentVolumeReclaimPolicy: Retain
  hostPath:
    path: /data/fitcheck
    type: DirectoryOrCreate
